# Continuous-Deploy

```bash
$ curl https://get.okteto.com -sSfL | sh   
$ wget -O /usr/local/bin/okteto https://github.com/okteto/okteto/releases/latest/download/okteto-Linux-x86_64 && \
  chmod +x /usr/local/bin/okteto
```
